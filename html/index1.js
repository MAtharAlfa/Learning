var jmlKelas = 7,
  kelasBerfungsi = 4,
  noKelas = 1;
while (noKelas <= kelasBerfungsi) {
  document.write(`Speaker di kelas ${noKelas} berfungsi dengan baik<br>`);
  noKelas++;
}
for (var noKelas = 5; noKelas <= 7; noKelas++) {
  document.write(`Speaker di kelas ${noKelas} berfungsi dengan baik<br>`);
}
